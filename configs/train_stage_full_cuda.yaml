experiment_name: stage_full_cuda
seed: 2026
device: cuda
allow_cpu_fallback: false
model_type: advanced
board_size: 15
rule_mode: basic
tactical_games: 2000
use_augmentation: true
use_auxiliary_loss: true
pretrain_epochs: 20
finetune_epochs: 10
batch_size: 128
learning_rate: 0.0005
weight_decay: 0.0001
num_simulations: 200
selfplay_games: 500
benchmark_games: 100
checkpoint_dir: outputs/checkpoints
log_dir: outputs/logs
output_dir: outputs/experiments
resume_from:
promote: false
loss_weights:
  policy: 1.0
  value: 1.0
  threat: 0.3
  forbidden: 0.2
  tactical_score: 0.1
scheduler: cosine
warmup_epochs: 2
grad_clip: 5.0
mixed_precision: true
max_moves: 180
